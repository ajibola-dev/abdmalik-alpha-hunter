# Alpha Hunter dependencies
# v0.4 — aiohttp added for async research pipeline

requests==2.31.0
beautifulsoup4==4.12.3
python-dotenv==1.0.0

# v0.4: async HTTP — install for full async research pipeline
# Falls back gracefully to sync if not present
aiohttp==3.9.5
